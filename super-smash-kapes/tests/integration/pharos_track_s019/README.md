# Sprint 019 runtime driver lab

This is an isolated QA continuation. It preserves the physical vehicle and imported GLB collision path. The current copied harness still has the S018 two-second micro-window; full-route pure-pursuit implementation remains the next blocker and no production controller is changed.
